{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//regions": "A `regions` override (fra1, next to the Neon DB in Frankfurt) would cut query latency, but this Vercel plan rejects custom function regions — every deploy that set it errored at config validation before building. Left out until the plan supports it. Search speed instead comes from the pg_trgm indexes, parallelised queries and edge caching.",
  "crons": [
    {
      "path": "/api/cron/scrape-offers?supermarket=panda",
      "schedule": "0 4 * * *"
    },
    {
      "path": "/api/cron/scrape-offers?supermarket=danube",
      "schedule": "0 5 * * *"
    },
    {
      "path": "/api/cron/scrape-offers?supermarket=bindawood",
      "schedule": "0 6 * * *"
    },
    {
      "path": "/api/cron/scrape-offers?supermarket=extra",
      "schedule": "0 7 * * *"
    },
    {
      "path": "/api/cron/scrape-offers?supermarket=alothaim",
      "schedule": "0 8 * * *"
    },
    {
      "path": "/api/cron/expire-flyers",
      "schedule": "0 0 * * *"
    },
    {
      "path": "/api/cron/cleanup-expired",
      "schedule": "0 1 * * 0"
    }
  ]
}
